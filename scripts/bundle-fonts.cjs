// Optional maintenance script. Runtime has no network dependency.
const fs=require('node:fs'),path=require('node:path');
(async()=>{const root=path.join(__dirname,'../assets/fonts');fs.mkdirSync(root,{recursive:true});const url='https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,400;0,500;0,600;1,400;1,500&family=Manrope:wght@400;500;600;700&display=swap';const response=await fetch(url,{headers:{'User-Agent':'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/130.0.0.0 Safari/537.36'}});if(!response.ok)throw Error('Font stylesheet unavailable');let css=await response.text();const urls=[...new Set([...css.matchAll(/https:\/\/fonts\.gstatic\.com\/[^)]+/g)].map(m=>m[0]))];for(let i=0;i<urls.length;i++){const u=urls[i],r=await fetch(u);if(!r.ok)throw Error('Font unavailable');const file='font-'+i+'.woff2';fs.writeFileSync(path.join(root,file),Buffer.from(await r.arrayBuffer()));css=css.split(u).join(file);}fs.writeFileSync(path.join(root,'fonts.css'),css);console.log('Bundled '+urls.length+' font subsets');})().catch(e=>{console.error(e);process.exitCode=1;});
